services:
  - type: web
    name: granja-azul
    runtime: php          ← ESTO le dice a Render que use PHP
    plan: free
    buildCommand: echo "Building PHP application"
    startCommand: php -S 0.0.0.0:$PORT
    envVars:
      - key: PHP_VERSION
        value: "8.1"
